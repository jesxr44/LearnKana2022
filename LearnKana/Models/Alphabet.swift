//
//  Alphabet.swift
//  LearnKana
//
//  Created by Jesus Escribano on 16/3/22.
//

import Foundation

protocol Alphabet
{
    var alphabet : Array<Array<String>> { get }

    var vowels : Array<String> { get }
    var kRow : Array<String> { get }
    var sRow : Array<String> { get }
    var tRow : Array<String> { get }
    var nRow : Array<String> { get }
    var hRow : Array<String> { get }
    var mRow : Array<String> { get }
    var yRow : Array<String> { get }
    var rRow : Array<String> { get }
    var wnRow : Array<String> { get }
    var gRow : Array<String> { get }
    var zRow : Array<String> { get }
    var dRow : Array<String> { get }
    var bRow : Array<String> { get }
    var pRow : Array<String> { get }
    
    // combinations kya, kyu, kyo, etc
    
    // random from row
    
    // random from any
    
    
}
